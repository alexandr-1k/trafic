Коротко о работе проекта:
В проекте есть бд, в которую просто добавляются данные об уже обработанных файлах
Через app.py запускается прога и в бесконечном цикле с помощью библиотеки aioscedule проверяет наличие файлов
1) сначала идет проверка файлов из clear data, если есть то отправляются в канал в тг(специальный файл для работы
    я не приложил в целях безопасности, так как через него идет авторизация аккаунта тг)
2) затем идет проверка папки dumps_data в которую распаковываются дампы, если такой файл есть, то запускается скрипт,
    который обрабатывает эти файлы и сохраняет в clear data два файла, первый - это полный файл со всеми ip из дампа,
    второй - это первые 100 ip("самые активные")
3) и в последнюю очередь проверяется наличие дампа в папке dumps, если он есть, то распаковывается в dumps data
И соответственно, после выполнения каждого из пунктов, данные об обработанных файлах заносятся в бд, чтобы они потом
    снова не обраотались. Чтобы с этим не заморачиваться, все эти данные можно сохранять в csv и обойтись без использования
    базы, так даже проще будет
Приверы работы скрипта есть в папке dumps data
Для работы нужны файлы .env (в нем указываются переменные среды для бд и для аккаунта тг) и .session - файл авторизации
    в тг через библиотеку pyrogram